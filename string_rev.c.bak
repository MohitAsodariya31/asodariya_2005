#include<stdio.h>
int main()
{
		char str1[100],str2[100];
		int i,j=0;
		printf("enter str :");
		gets(str1);
		printf("enter str2");
		gets(str2);
		for(i=str[str]-1;i<0;i--)
		{
				str2[j]=str[i];
				j++;
		}
		str[j]=!'\0';
		printf("\n reverse string is : %s",str2);
		
}