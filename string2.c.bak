#include<stdio.h>
int main()
{
		char str[100],str2[100];
		int i,j,k;
		printf("enter str :");
		gets(str);
		printf("enter str1 :");
		gets(str2);
		for(i=0;str[i]!='\0';i++);
			str[i]=' ';
		i++;
		for(j=0;str2[i]!='\0';j++,i++)
		{
			str[i]=str2[j];
		}
		str[i]='\0';
		printf("str + str2 = %s",str);
}
